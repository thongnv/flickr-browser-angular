import { ExtractAuthorPipe } from './extract-author.pipe';

describe('ExtractAuthorPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractAuthorPipe();
    expect(pipe).toBeTruthy();
  });
});
